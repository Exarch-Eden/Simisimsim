
/** The dimensions of the map in pixels. */
// const MAP_WIDTH = 400;
const MAP_WIDTH = window.screen.width;
// const MAP_HEIGHT = 400;
const MAP_HEIGHT = window.screen.height;
// old value
// const MAP_DIMENSIONS = 400;
/** The lowest possible number of nodes in the map. */
const MIN_NODES = 4;
/** The highest possible number of nodes in the map. */
const MAX_NODES = 128;

export {
    // MAP_DIMENSIONS,
    MAP_WIDTH,
    MAP_HEIGHT,
    MIN_NODES,
    MAX_NODES
}
